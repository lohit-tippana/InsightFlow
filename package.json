{
  "name": "insightflow",
  "version": "1.0.0",
  "private": true,
  "description": "InsightFlow — AI-powered real-time product analytics & intelligence platform",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install && npm --prefix sdk install",
    "dev:backend": "npm --prefix backend run dev",
    "dev:worker": "npm --prefix backend run dev:worker",
    "dev:frontend": "npm --prefix frontend run dev",
    "build:sdk": "npm --prefix sdk run build",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "db:migrate": "npm --prefix backend run db:migrate",
    "db:seed": "npm --prefix backend run db:seed",
    "db:reset": "npm --prefix backend run db:reset",
    "test": "npm --prefix backend test",
    "typecheck": "npm --prefix backend run typecheck && npm --prefix frontend run typecheck && npm --prefix sdk run typecheck"
  }
}
